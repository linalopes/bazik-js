/**
 * Optional preset behavior toggles.
 * Defaults preserve legacy behavior (no automatic default application).
 */
export const PRESET_BEHAVIOR_CONFIG = {
  applyDefaultsOnSelect: false,
  applyDefaultColorsOnSelect: false,
} as const;

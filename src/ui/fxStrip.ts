/**
 * FX strip UI is Svelte-driven ({@link ./components/FxStrip.svelte}).
 * This module re-exports the controller/learn hook so `InputRouter` stays unchanged.
 */
export { toggleFxArmById } from '../core/state/fxStore';
